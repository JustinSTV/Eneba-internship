import styled from "styled-components";

const HeaderComponent = styled.header``;

const Header = () => {
  return (
    <HeaderComponent>
      <h1>Eneba</h1>
    </HeaderComponent>
  );
};

export default Header;
